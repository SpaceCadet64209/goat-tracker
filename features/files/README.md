# Files feature

This feature owns private farm-file validation, generated storage paths, upload,
short-lived download links, and retryable deletion reconciliation. It accepts
only JPEG, PNG, and PDF files up to 10 MB. Never derive an object path from a
user-supplied filename.
