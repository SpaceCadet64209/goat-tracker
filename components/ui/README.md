# UI primitives

This directory contains application-owned shadcn/ui primitives. Components may be generated as a starting point, but their source is reviewed and committed here. Feature-specific composition belongs under `features/` or `components/app-shell/`.
